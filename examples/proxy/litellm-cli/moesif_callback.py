# Loaded by LiteLLM proxy via proxy_config.yaml `callbacks` setting.
# Set MOESIF_APPLICATION_ID env var before starting the proxy.
from moesif_litellm import MoesifHandler

moesif_handler = MoesifHandler()